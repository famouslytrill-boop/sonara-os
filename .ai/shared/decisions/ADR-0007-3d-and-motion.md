# ADR-0007: 3D and motion
Status: ACCEPTED ladder. CSS/SVG → Canvas 2D (shipped engine:
sonara-interface-engine.js with reduced-motion/visibility/DPR/save-data guards;
never requests WebGPU adapter at load) → WebGL/Three.js optional (not shipped).
Canvas product-identity scenes = CLAUDE-3 backlog. No motion may block auth,
checkout, or navigation.

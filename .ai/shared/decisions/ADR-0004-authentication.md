# ADR-0004: Authentication
Status: STUB — owner Codex. Current: Supabase email/password via server routes
(/auth/login, /auth/signup), cookie session, server-side admin verification,
Google OAuth deferred. Directive §11 additions (magic link, OTP, passkeys, MFA)
need provider verification before any UI shows them.

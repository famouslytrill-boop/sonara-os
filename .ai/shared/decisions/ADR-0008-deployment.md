# ADR-0008: Deployment
Status: ACCEPTED baseline. GitHub main → Vercel production; pnpm frozen
lockfile; vercel-build runs apply:runtime; SW version tied to asset token.
GitLab mirror / Docker / Rancher roles = Codex backlog (directive §20); the
customer web frontend stays on Vercel absent a new ADR.

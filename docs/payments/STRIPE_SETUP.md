# Stripe Setup

Stripe remains setup-gated. Required values include `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`, and price IDs. Webhooks must be verified server-side and fulfillment must not rely only on redirect success.

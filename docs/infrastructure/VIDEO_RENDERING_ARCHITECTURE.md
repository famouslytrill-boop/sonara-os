# Video Rendering Architecture

Future video rendering should use authenticated jobs, queues, quotas, private storage, render-worker health checks, audit logs, and owner approval for publishing. Heavy FFmpeg or GPU tasks must be bounded and observable.

# App Code Architecture

Domain policy belongs in `lib`, registry data belongs in `data`, reusable UI belongs in `components`, and route surfaces belong in `app`. Scaffolds stay gated until provider setup and review are complete.

# Web App Architecture

The Next.js app uses public routes for SONARA products and private `/app` routes for dashboards, settings, admin, and owner systems. Public pages must render without private provider secrets.

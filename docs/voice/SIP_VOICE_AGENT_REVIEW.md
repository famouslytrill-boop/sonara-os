# SIP Voice Agent Review

SIP voice-agent handoff is review-only. It requires consent, a visible listening indicator, a human escalation path, provider approval, call-record policy, and server-side credential handling.

No voice agent may place calls automatically or claim emergency dispatch support.

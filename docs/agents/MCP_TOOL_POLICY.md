# MCP Tool Policy

MCP-style tools are allowed only through an allowlist. Tools must not expose secrets, bypass provider terms, access production systems without approval, or run unsafe customer-facing actions.

# Install Required Tools on Windows

Normal SONARA OSâ„¢ users do not need to install developer tools. They should use the browser or PWA.

Founder/developer tools:

- Node.js LTS
- npm
- Git
- Vercel CLI if using CLI deploys
- Stripe CLI if testing webhooks locally
- Supabase CLI if applying migrations locally

Check local tooling:

```powershell
npm run check:software
```

Install only from official sources. Do not install heavy optional audio, GPU, or vector systems for launch unless a specific task requires them.

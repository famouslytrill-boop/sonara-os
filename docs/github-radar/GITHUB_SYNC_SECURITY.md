# GitHub Sync Security

`GITHUB_TOKEN` is optional and server-only. If it is missing, GitHub Radar runs in manual registry mode. Tokens must never be logged, sent to the client, stored in repository metadata, or embedded in generated prompts.

# Supabase Vault Policy

Vault is planned for secret references and provider credential management. Application tables may store secret reference IDs only, not secret values. Secrets must never appear in client bundles, logs, support exports, or public docs.

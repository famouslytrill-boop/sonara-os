# Supabase Edge Functions Integrations

Edge Functions are reserved for reviewed server-side work such as support email dispatch, webhook handling, provider health checks, and background task entry points. Secrets remain in provider environments and must be redacted from logs.

"use strict";

// Machine-readable defaults used by diagnostics and contract tests. Keeping
// this object explicit also makes the public preference behavior reviewable
// without depending on source-code minification or formatting.
globalThis.SONARA_NEXUS_DEFAULT_PREFERENCES = Object.freeze({language:"en",sound:"off",haptics:"on"});

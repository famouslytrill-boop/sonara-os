export const phoneSystemRiskPolicy = Object.freeze({
  blockedUses: Object.freeze([
    "robocalling",
    "covert recording",
    "blocked emergency dispatch claims",
    "employee/customer monitoring",
    "hidden call logging",
    "SIP credential exposure"
  ])
});

export const voipProviderPolicy = Object.freeze({
  productionIntegrated: false,
  proprietaryLicenseRequiredForClosedSource: true,
  callProviderApprovalRequired: true,
  publicClaimsAllowed: Object.freeze(["reference only", "planning", "review required"])
});

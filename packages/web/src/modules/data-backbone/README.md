# Data Backbone

Supabase is the source of truth, Prisma is the ORM contract, and tenant guards deny cross-organization access by default.

Disabled scaffold: no external code, no secrets, organization_id required, RBAC/audit required before live use.

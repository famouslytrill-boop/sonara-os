# Launch Ops

Launch requires required env validation, no client service-role exposure, rollback notes, and owner approval.

Disabled scaffold: no external code, no secrets, organization_id required, RBAC/audit required before live use.

# AI Provider Router

Provider routing uses official APIs only, with cost, privacy, health, and blocklist policy checks.

Disabled scaffold: no external code, no secrets, organization_id required, RBAC/audit required before live use.

import type { EmergingCreatorPlan } from "./emergingCreatorTypes";

export function buildEmergingCreatorPlan(): EmergingCreatorPlan {
  return {
    lane: "Minimum-input guided creation for emerging music creators.",
    firstActions: ["Create first project", "Run SONARA Core™", "Add lyric/writing details", "Export a prompt pack", "Review store/pricing"],
    trustNotes: ["No fake follower growth.", "No fake revenue promise.", "Creator owns the responsibility for rights clearance."],
  };
}

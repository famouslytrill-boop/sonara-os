-- Member-scoped SELECT policies for the tables the application reads.
--
-- GENERATED by scripts/generate-member-read-policies.cjs -- regenerate rather
-- than edit by hand.
--
-- Prerequisite for CRIT-3 item (2), user-scoped reads. Before this migration,
-- 184 of 206 tenant-scoped tables had no policy a signed-in user could read
-- through, so forwarding a caller's JWT would have returned zero rows and
-- blanked the workspace screens.
--
-- This migration is additive and cannot change existing behaviour:
--
--   * service_role bypasses RLS entirely, and every current query uses it
--   * nothing currently connects as `authenticated`
--   * no existing policy is dropped or altered; the policies created here have
--     their own names and only grant SELECT
--
-- Applying it makes user-scoped reads possible. It does not switch any read
-- over -- that is a separate, verifiable step once this has landed.

do $$
begin
  if to_regclass('public.organization_memberships') is null then
    raise exception 'organization_memberships is missing; member policies cannot be created';
  end if;
  if not exists (
    select 1 from pg_proc p
    join pg_namespace n on n.oid = p.pronamespace
    where n.nspname = 'public' and p.proname = 'is_org_member'
  ) then
    raise exception 'public.is_org_member() is missing; member policies would deny every row';
  end if;
end
$$;

-- Organization-scoped: any member of the owning organization may read.

do $$
begin
  if to_regclass('public.audio_assets') is null then
    raise notice 'skipping audio_assets: table not present';
    return;
  end if;

  execute 'alter table public.audio_assets enable row level security';
  execute 'drop policy if exists "audio_assets_select_member" on public.audio_assets';
  execute 'create policy "audio_assets_select_member" on public.audio_assets for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.automation_rules') is null then
    raise notice 'skipping automation_rules: table not present';
    return;
  end if;

  execute 'alter table public.automation_rules enable row level security';
  execute 'drop policy if exists "automation_rules_select_member" on public.automation_rules';
  execute 'create policy "automation_rules_select_member" on public.automation_rules for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.business_workspaces') is null then
    raise notice 'skipping business_workspaces: table not present';
    return;
  end if;

  execute 'alter table public.business_workspaces enable row level security';
  execute 'drop policy if exists "business_workspaces_select_member" on public.business_workspaces';
  execute 'create policy "business_workspaces_select_member" on public.business_workspaces for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.creator_generation_jobs') is null then
    raise notice 'skipping creator_generation_jobs: table not present';
    return;
  end if;

  execute 'alter table public.creator_generation_jobs enable row level security';
  execute 'drop policy if exists "creator_generation_jobs_select_member" on public.creator_generation_jobs';
  execute 'create policy "creator_generation_jobs_select_member" on public.creator_generation_jobs for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.employee_time_entries') is null then
    raise notice 'skipping employee_time_entries: table not present';
    return;
  end if;

  execute 'alter table public.employee_time_entries enable row level security';
  execute 'drop policy if exists "employee_time_entries_select_member" on public.employee_time_entries';
  execute 'create policy "employee_time_entries_select_member" on public.employee_time_entries for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.growth_audience_segments') is null then
    raise notice 'skipping growth_audience_segments: table not present';
    return;
  end if;

  execute 'alter table public.growth_audience_segments enable row level security';
  execute 'drop policy if exists "growth_audience_segments_select_member" on public.growth_audience_segments';
  execute 'create policy "growth_audience_segments_select_member" on public.growth_audience_segments for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.growth_campaigns') is null then
    raise notice 'skipping growth_campaigns: table not present';
    return;
  end if;

  execute 'alter table public.growth_campaigns enable row level security';
  execute 'drop policy if exists "growth_campaigns_select_member" on public.growth_campaigns';
  execute 'create policy "growth_campaigns_select_member" on public.growth_campaigns for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.growth_contact_consents') is null then
    raise notice 'skipping growth_contact_consents: table not present';
    return;
  end if;

  execute 'alter table public.growth_contact_consents enable row level security';
  execute 'drop policy if exists "growth_contact_consents_select_member" on public.growth_contact_consents';
  execute 'create policy "growth_contact_consents_select_member" on public.growth_contact_consents for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.growth_content_queue') is null then
    raise notice 'skipping growth_content_queue: table not present';
    return;
  end if;

  execute 'alter table public.growth_content_queue enable row level security';
  execute 'drop policy if exists "growth_content_queue_select_member" on public.growth_content_queue';
  execute 'create policy "growth_content_queue_select_member" on public.growth_content_queue for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.growth_conversions') is null then
    raise notice 'skipping growth_conversions: table not present';
    return;
  end if;

  execute 'alter table public.growth_conversions enable row level security';
  execute 'drop policy if exists "growth_conversions_select_member" on public.growth_conversions';
  execute 'create policy "growth_conversions_select_member" on public.growth_conversions for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.growth_experiments') is null then
    raise notice 'skipping growth_experiments: table not present';
    return;
  end if;

  execute 'alter table public.growth_experiments enable row level security';
  execute 'drop policy if exists "growth_experiments_select_member" on public.growth_experiments';
  execute 'create policy "growth_experiments_select_member" on public.growth_experiments for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.growth_leads') is null then
    raise notice 'skipping growth_leads: table not present';
    return;
  end if;

  execute 'alter table public.growth_leads enable row level security';
  execute 'drop policy if exists "growth_leads_select_member" on public.growth_leads';
  execute 'create policy "growth_leads_select_member" on public.growth_leads for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.growth_metric_snapshots') is null then
    raise notice 'skipping growth_metric_snapshots: table not present';
    return;
  end if;

  execute 'alter table public.growth_metric_snapshots enable row level security';
  execute 'drop policy if exists "growth_metric_snapshots_select_member" on public.growth_metric_snapshots';
  execute 'create policy "growth_metric_snapshots_select_member" on public.growth_metric_snapshots for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.growth_provider_jobs') is null then
    raise notice 'skipping growth_provider_jobs: table not present';
    return;
  end if;

  execute 'alter table public.growth_provider_jobs enable row level security';
  execute 'drop policy if exists "growth_provider_jobs_select_member" on public.growth_provider_jobs';
  execute 'create policy "growth_provider_jobs_select_member" on public.growth_provider_jobs for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.growth_touchpoints') is null then
    raise notice 'skipping growth_touchpoints: table not present';
    return;
  end if;

  execute 'alter table public.growth_touchpoints enable row level security';
  execute 'drop policy if exists "growth_touchpoints_select_member" on public.growth_touchpoints';
  execute 'create policy "growth_touchpoints_select_member" on public.growth_touchpoints for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.inventory_items') is null then
    raise notice 'skipping inventory_items: table not present';
    return;
  end if;

  execute 'alter table public.inventory_items enable row level security';
  execute 'drop policy if exists "inventory_items_select_member" on public.inventory_items';
  execute 'create policy "inventory_items_select_member" on public.inventory_items for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.location_events') is null then
    raise notice 'skipping location_events: table not present';
    return;
  end if;

  execute 'alter table public.location_events enable row level security';
  execute 'drop policy if exists "location_events_select_member" on public.location_events';
  execute 'create policy "location_events_select_member" on public.location_events for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.market_intelligence_competitors') is null then
    raise notice 'skipping market_intelligence_competitors: table not present';
    return;
  end if;

  execute 'alter table public.market_intelligence_competitors enable row level security';
  execute 'drop policy if exists "market_intelligence_competitors_select_member" on public.market_intelligence_competitors';
  execute 'create policy "market_intelligence_competitors_select_member" on public.market_intelligence_competitors for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.market_intelligence_opportunities') is null then
    raise notice 'skipping market_intelligence_opportunities: table not present';
    return;
  end if;

  execute 'alter table public.market_intelligence_opportunities enable row level security';
  execute 'drop policy if exists "market_intelligence_opportunities_select_member" on public.market_intelligence_opportunities';
  execute 'create policy "market_intelligence_opportunities_select_member" on public.market_intelligence_opportunities for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.market_intelligence_reviews') is null then
    raise notice 'skipping market_intelligence_reviews: table not present';
    return;
  end if;

  execute 'alter table public.market_intelligence_reviews enable row level security';
  execute 'drop policy if exists "market_intelligence_reviews_select_member" on public.market_intelligence_reviews';
  execute 'create policy "market_intelligence_reviews_select_member" on public.market_intelligence_reviews for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.market_intelligence_segments') is null then
    raise notice 'skipping market_intelligence_segments: table not present';
    return;
  end if;

  execute 'alter table public.market_intelligence_segments enable row level security';
  execute 'drop policy if exists "market_intelligence_segments_select_member" on public.market_intelligence_segments';
  execute 'create policy "market_intelligence_segments_select_member" on public.market_intelligence_segments for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.market_intelligence_signals') is null then
    raise notice 'skipping market_intelligence_signals: table not present';
    return;
  end if;

  execute 'alter table public.market_intelligence_signals enable row level security';
  execute 'drop policy if exists "market_intelligence_signals_select_member" on public.market_intelligence_signals';
  execute 'create policy "market_intelligence_signals_select_member" on public.market_intelligence_signals for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.menu_items') is null then
    raise notice 'skipping menu_items: table not present';
    return;
  end if;

  execute 'alter table public.menu_items enable row level security';
  execute 'drop policy if exists "menu_items_select_member" on public.menu_items';
  execute 'create policy "menu_items_select_member" on public.menu_items for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.motion_sensor_events') is null then
    raise notice 'skipping motion_sensor_events: table not present';
    return;
  end if;

  execute 'alter table public.motion_sensor_events enable row level security';
  execute 'drop policy if exists "motion_sensor_events_select_member" on public.motion_sensor_events';
  execute 'create policy "motion_sensor_events_select_member" on public.motion_sensor_events for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.music_projects') is null then
    raise notice 'skipping music_projects: table not present';
    return;
  end if;

  execute 'alter table public.music_projects enable row level security';
  execute 'drop policy if exists "music_projects_select_member" on public.music_projects';
  execute 'create policy "music_projects_select_member" on public.music_projects for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.product_lifecycle_initiatives') is null then
    raise notice 'skipping product_lifecycle_initiatives: table not present';
    return;
  end if;

  execute 'alter table public.product_lifecycle_initiatives enable row level security';
  execute 'drop policy if exists "product_lifecycle_initiatives_select_member" on public.product_lifecycle_initiatives';
  execute 'create policy "product_lifecycle_initiatives_select_member" on public.product_lifecycle_initiatives for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.recipe_cards') is null then
    raise notice 'skipping recipe_cards: table not present';
    return;
  end if;

  execute 'alter table public.recipe_cards enable row level security';
  execute 'drop policy if exists "recipe_cards_select_member" on public.recipe_cards';
  execute 'create policy "recipe_cards_select_member" on public.recipe_cards for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.tactile_events') is null then
    raise notice 'skipping tactile_events: table not present';
    return;
  end if;

  execute 'alter table public.tactile_events enable row level security';
  execute 'drop policy if exists "tactile_events_select_member" on public.tactile_events';
  execute 'create policy "tactile_events_select_member" on public.tactile_events for select to authenticated using (public.is_org_member(organization_id))';
end
$$;

do $$
begin
  if to_regclass('public.vendor_invoices') is null then
    raise notice 'skipping vendor_invoices: table not present';
    return;
  end if;

  execute 'alter table public.vendor_invoices enable row level security';
  execute 'drop policy if exists "vendor_invoices_select_member" on public.vendor_invoices';
  execute 'create policy "vendor_invoices_select_member" on public.vendor_invoices for select to authenticated using (public.is_org_member(organization_id))';
end
$$;


-- Person-scoped: these rows belong to one member, not to the organization.

do $$
begin
  if to_regclass('public.business_employee_profiles') is null then
    raise notice 'skipping business_employee_profiles: table not present';
    return;
  end if;

  execute 'alter table public.business_employee_profiles enable row level security';
  execute 'drop policy if exists "business_employee_profiles_select_own" on public.business_employee_profiles';
  execute 'create policy "business_employee_profiles_select_own" on public.business_employee_profiles for select to authenticated using (auth.uid() = user_id)';
end
$$;

do $$
begin
  if to_regclass('public.sonara_platforms') is null then
    raise notice 'skipping sonara_platforms: table not present';
    return;
  end if;

  execute 'alter table public.sonara_platforms enable row level security';
  execute 'drop policy if exists "sonara_platforms_select_own" on public.sonara_platforms';
  execute 'create policy "sonara_platforms_select_own" on public.sonara_platforms for select to authenticated using (auth.uid() = user_id)';
end
$$;

do $$
begin
  if to_regclass('public.user_notifications') is null then
    raise notice 'skipping user_notifications: table not present';
    return;
  end if;

  execute 'alter table public.user_notifications enable row level security';
  execute 'drop policy if exists "user_notifications_select_own" on public.user_notifications';
  execute 'create policy "user_notifications_select_own" on public.user_notifications for select to authenticated using (auth.uid() = user_id)';
end
$$;

do $$
begin
  if to_regclass('public.user_preferences') is null then
    raise notice 'skipping user_preferences: table not present';
    return;
  end if;

  execute 'alter table public.user_preferences enable row level security';
  execute 'drop policy if exists "user_preferences_select_own" on public.user_preferences';
  execute 'create policy "user_preferences_select_own" on public.user_preferences for select to authenticated using (auth.uid() = user_id)';
end
$$;

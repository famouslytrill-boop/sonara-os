import { Card } from "@/components/ui/Card";

export function ProjectsPanel() {
  return (
    <Card title="Projects">
      <p className="text-sm leading-6 text-slate-300">
        Music projects stay inside the SONARA One app scope with their own workspace, history,
        readiness notes, and export status.
      </p>
    </Card>
  );
}

export function PaymentProviderNotice() {
  return <p className="rounded-2xl border border-white/10 bg-white/[0.04] p-5 text-sm text-[#CBD5E1]">Payment providers remain setup-gated. SONARA does not store raw card data or CVV.</p>;
}

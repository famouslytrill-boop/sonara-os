export function ContactImportPanel() {
  return <p className="rounded-2xl border border-white/10 bg-white/[0.04] p-5 text-sm text-[#CBD5E1]">Contact import requires explicit user selection and consent. No automatic imports run.</p>;
}

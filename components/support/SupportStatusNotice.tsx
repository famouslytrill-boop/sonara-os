export function SupportStatusNotice() {
  return (
    <p className="rounded-2xl border border-white/10 bg-white/[0.04] p-5 text-sm leading-6 text-[#CBD5E1]">
      Support intake validates requests, assigns a reference ID, and can store them when Supabase is configured. Email
      notifications are sent only after Resend and the real support inboxes are verified.
    </p>
  );
}

export function ToastCenter() {
  return (
    <div aria-live="polite" className="sr-only">
      Toast notifications appear here when a configured action creates a client-safe status message.
    </div>
  );
}

{
  "name": "@sonara/songsmith",
  "version": "0.1.0",
  "private": true,
  "description": "A self-hosted web app that turns a text idea into a song. Approval-gated accounts, SQLite storage, RunPod for generation, no dependencies.",
  "license": "MIT",
  "type": "commonjs",
  "main": "src/server.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/*.test.js"
  }
}

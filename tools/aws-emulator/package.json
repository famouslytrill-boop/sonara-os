{
  "name": "@sonara/aws-emulator",
  "version": "0.1.0",
  "private": true,
  "description": "A local AWS emulator on one port. No auth, no tokens, no feature gates, no dependencies.",
  "license": "UNLICENSED",
  "type": "commonjs",
  "main": "src/server.js",
  "bin": { "aws-emulator": "src/server.js" },
  "engines": { "node": ">=20" },
  "scripts": { "start": "node src/server.js", "test": "node --test tests/" }
}

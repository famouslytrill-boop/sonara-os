# Runtime: nothing. The tool uses only the standard library, on purpose --
# a blocklist maintenance script that cannot run because a dependency moved
# is a blocklist that stops being maintained.
#
# Tests only:
# Explicit security pin: avoid the vulnerable Pygments 2.9.0 resolution reported by OSV.
Pygments==2.21.0
pytest==9.0.3

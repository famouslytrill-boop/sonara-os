export const paymentPolicy = {
  blocked: ["raw card storage", "CVV storage", "custody of funds claims", "automatic charges without consent", "hidden fees"],
  allowed: ["Stripe Checkout", "hosted billing portal", "payment link planning", "server-side webhook verification"],
};
